<html>
  <head>
    <title>BookMark</title>
    <!--Meta Tags-->
    <meta http-equiv="Content-Type" content="text/html;charset=UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!--Bootstrap CSS-->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <!--Header-->
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a href="/" class="navbar-brand">BookMark</a>
          <a href="/checkout/cart" class="btn btn-outline-light">Carrinho</a>
        </div>
      </nav>
    </header>
    <!--Conteudo-->
    <main class="container">
      <h1 class="mt-3 text-center">Minha Conta</h1>
      <p class="mt-3 text-center">
        Olá, <%= user.name %>! Caso queira alterar seus dados veja a seguir:
      </p>
      <!--Dados do Usuário-->
      <form>
        <div class="mb-3">
          <label for="name" class="form-label">Nome</label>
          <input
            type="text"
            class="form-control"
            id="name"
            name="name"
            value="<%= user.name %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input
            type="email"
            class="form-control"
            id="email"
            name="email"
            value="<%= user.email %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="old-password" class="form-label">Senha Antiga</label>
          <input
            type="password"
            class="form-control"
            id="old-password"
            name="oldPassword"
            required
          />
        </div>
        <div class="mb-3">
          <label for="new-password" class="form-label">Nova Senha</label>
          <input
            type="password"
            class="form-control"
            id="new-password"
            name="newPassword"
          />
        </div>
        <button type="submit" class="btn btn-dark">Salvar</button>
      </form>
      <nav class="mt-5">
        <a href="/" class="btn btn-dark">Voltar</a>
        <% if (user.admin) { %>
        <a href="/admin/orders" class="btn btn-dark">Ver Todos os Pedidos</a>
        <% } %>
        <a href="/account/me/cards" class="btn btn-dark">Meus Cartões</a>
        <a href="/account/me/orders" class="btn btn-dark">Meus Pedidos</a>
        <a href="/delete" class="btn btn-danger">Excluir Conta</a>
        <a href="/login/logout" class="btn btn-danger">Sair</a>
      </nav>
    </main>
    <!--Bootstrap JS-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
